.idea/
build/
node_modules/
web/node_modules/
web/dist/
# iOS: build web copiado desde dist/ (regenerable con cap copy), solo en disco
web/ios/App/App/public/
web/*.tsbuildinfo
OPENCODE-REPO.md
# Changelogs locales por versión (no se suben a GitHub)
/CHANGELOG-v*.md
# Android native project has its own .gitignore
# web/android/ is NOT ignored — the Android .gitignore handles build artifacts

# OS files
.DS_Store
Thumbs.db

# Env files
.env
.env.local
.env.*.local
*.keystore
*.jks
*.p12
key.properties
keystore.properties
*play-service-account*.json
service-account*.json
secrets.json

# Play Store / release artifacts (no commitear)
*.aab
*.apk
/OpenHer-debug.apk
OpenHer-release.aab
play/
fastlane/
web/android/keystore/
PLAY-STORE.md
web/scripts/play-upload.py
web/scripts/gen-play-assets.py
web/scripts/upload-tmpfiles.py
web/scripts/check-pkg.py
.playwright-mcp/
play-store-assets/
editor-*.png

# Logs
*.log
npm-debug.log*

# Editor backups
*~
*.swp
*.swo

# Personal / legacy files (not tracked)
BUILD_AND_INSTALL.md
web/deploy-ngrok.ps1
.opencode/
.sisyphus/
implementation_plan.md
walkthrough.md
tmpfiles-page.png
architecture.html
architecture.json
web/android/build-log.txt
web/dist-stale/

signaling-worker/node_modules/
signaling-worker/.wrangler/

# OpenDesign externo (vendorizado eliminado, vive en G:/proyectos/open-design)
od-web/
desktop-app/target/
desktop-app/data/
desktop-app/opencode-desktop.exe
desktop-app/openher-desktop.exe

# Build artifacts desktop (regenerables con scripts/build-desktop.ps1)
dist-desktop/
web/dist-locked/
web/dist-locked2/

# Artefactos de build Rust (raíz del repo; desktop-app/target ya está arriba)
target/

# Contenido de aprendizaje personal (forense/papers): NO se sube a GitHub.
# Queda en disco (web/dist lo copia al build), solo se destrackea del repo.
web/public/learning/
web/learning-raw/

# Evidencia generada del harness de evals de debate (se regenera con --dry-run)
scripts/debate-eval/out/
scripts/debate-eval/out-*/

# Evidencia del harness de evals de equipo (se regenera corriendo run.mjs)
scripts/team-eval/out-*/

# Estado runtime por proyecto (registry/claims/feed/debates): nunca a git.
.openher/

# Documentación personal de gestión (scrum): NO se sube a GitHub.
docs/scrum/
docs/scrum-copia/

# Docs del repo (diseño, planes, marketing, notas locales): quedan en disco,
# no se suben a GitHub.
docs/

# Memoria de proyecto (mapa y bitácora): viven local, no se suben a GitHub.
PROJECT_MAP.md
PROJECT_MEMORY.md

# Evidencia HTML de debates (se regenera): queda en disco, no a git.
/.debates/

# Estado runtime del tablero teamwork (cambia solo): no a git.
.agents/teamwork_*.json
.agents/teamwork_*.md
# Artefactos de los gates de refactor (los escriben dentro de .agents/): no a git.
.agents/*.txt

# Copia local bloqueada por otro proceso (original en docs/local/): no a git.
openher-arquitectura.html
