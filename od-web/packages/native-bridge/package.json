{
  "name": "@open-design/native-bridge",
  "version": "0.19.2",
  "private": true,
  "type": "module",
  "description": "Native migration seam: pure TypeScript contracts for the daemon registries that a Rust/napi core can implement. Keeps the expensive scan/list paths behind a stable interface so the JS implementation can be swapped for a compiled backend without touching call sites.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "4.1.6"
  }
}
